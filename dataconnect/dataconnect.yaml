specVersion: "v1"
serviceId: "staylocal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "staylocal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
